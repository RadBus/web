// NOTE: this static file will not be served in production since it will be
//       overriden by the dynamic /js/api-config.js file served up by the
//       server-side api.coffee file

var apiBaseUrl = 'http://api.bus.targetrad.com/v1';
